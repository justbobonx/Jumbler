(() => {
  const WORD_LENS = [5, 6, 7, 8];
  const VERSION = window.VERSION || "0.6.2";
  const TICK_MS = 750;
  const LAST_TICK_MS = 1000;
  const BUZZ_ANIM_MS = 1000;
  const TICK_SHRINK_MS = 240;
  const PLAYERS = [
    { name: "red", fill: "#c23b3b", score: "#ffb4b4" },
    { name: "blue", fill: "#2b6cb0", score: "#b7d4ff" },
    { name: "yellow", fill: "#d4b22a", score: "#ffe7a3" },
    { name: "purple", fill: "#7a3db3", score: "#e0c2ff" },
  ];
  // file continues in repo after this restore - use artifacts copy
})();
